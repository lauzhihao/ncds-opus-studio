"""ncds-opus-core primitive 命令（文生图 / 图生图 / 视频 / 转写 / 改写 / 配音 / 两套渲染）。

这些是两端复用的底层能力；agent 编排留在 ncds_opus_factory.commands。
PRIMITIVE_REGISTRY 在 8 个 primitive 全部迁入后再建（见 docs/MONOREPO-SPLIT-DESIGN.md §2）。
"""
